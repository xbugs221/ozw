# Change Proposals

This directory is reserved for active change proposals only.

Archived proposals are intentionally not tracked in this release repository. Durable requirements live in `docs/specs/`, and their executable coverage lives under `tests/`.
