# PURPOSE: Template for per-instance secrets. Keep actual .env.zzl and
# .env.dmh files on the NAS only; never commit them to the source repository.

JWT_SECRET=replace-with-a-random-32-byte-secret
CREDENTIAL_ENCRYPTION_KEY=replace-with-a-random-32-byte-secret
