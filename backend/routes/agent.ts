/**
 * PURPOSE: Keep the external agent route entrypoint typed and small while delegating implementation.
 */
export { default, __agentRouteInternalsForTest } from '../domains/agent/agent-route-runtime.js';
