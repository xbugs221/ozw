/**
 * PURPOSE: Keep the frontend transcript adapter thin while shared runtime code
 * owns provider event reduction for both browser and backend callers.
 */

export * from '../../../../shared/provider-runtime-transcript.js';
