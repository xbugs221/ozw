/**
 * PURPOSE: Define the realtime session lifecycle split point for the chat runtime.
 */
export function describeRealtimeSessionLifecycleBoundary(): string {
  /** Describe the realtime lifecycle business boundary. */
  return 'realtime-session-lifecycle';
}
