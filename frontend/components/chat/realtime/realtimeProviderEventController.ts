/**
 * PURPOSE: Define the provider event controller split point for realtime chat handling.
 */
export function describeRealtimeProviderEventBoundary(): string {
  /** Describe the provider event business boundary. */
  return 'realtime-provider-event-controller';
}
