/**
 * PURPOSE: Define the manual session rendering split point for project overview.
 */
export function ProjectOverviewManualSessions() {
  /** Render no standalone UI until the existing panel delegates this boundary. */
  return null;
}
