/**
 * PURPOSE: Define the workflow section rendering split point for project overview.
 */
export function ProjectOverviewWorkflowSection() {
  /** Render no standalone UI until the existing panel delegates this boundary. */
  return null;
}
