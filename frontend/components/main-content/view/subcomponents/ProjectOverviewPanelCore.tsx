/**
 * PURPOSE: Keep the project overview compatibility boundary thin.
 * 业务目的：原路径只负责组合导出，真实总览运行时在 ProjectOverviewPanelRuntime 中维护。
 */
import ProjectOverviewPanelRuntime from './ProjectOverviewPanelRuntime';

export default ProjectOverviewPanelRuntime;
