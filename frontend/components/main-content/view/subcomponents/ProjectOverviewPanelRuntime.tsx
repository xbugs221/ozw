/**
 * PURPOSE: Preserve the legacy project overview module path while the real
 * runtime lives under the project-overview boundary.
 */

export { default } from '../../project-overview/ProjectOverviewPanelRuntime.impl';
